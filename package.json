{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build:render": "BASE_PATH=/ pnpm --filter @workspace/pump-scanner run build && pnpm --filter @workspace/api-server run build",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev": "node artifacts/api-server/dist/index.mjs &",
    "start": "echo \"Run: node artifacts/api-server/dist/index.mjs\"",
    "build:all": "node artifacts/api-server/build.mjs && vite build --config artifacts/pump-scanner/vite.config.ts"
  },
  "private": true,
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
